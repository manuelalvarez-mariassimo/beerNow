<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/meyer-reset/2.0/reset.css">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://kit.fontawesome.com/9c01b07d37.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.0/axios.min.js"></script>
</head>

<body class="pages profile-page">

    <div id="animation"></div>

    <nav>
        <a href="/"><img src="/images/beer-now.png" alt="BeerNow Logo" id="logo"></a>
        <div class="menu">
            <p>{{user.username}}</p>
        </div>
    </nav>

    <div class="imgContact pages">
        <img src="{{user.picture}}" alt="">
    </div>

    <div class="contact beer-head">
        <div class="name title is-2 has-text-weight-bold">
            <p>{{user.username}}</p>
        </div>

        <div class="location">
            <p class="locate-message"><img src="/images/map-pin.svg" alt=""><span>From
                    {{user.city}},{{user.country}}</span></p>
        </div>
    </div>
    <section class="favoriteBeers">
        <h2 class="title is-4">Your favorite beers</h2>
        <hr>

        {{#each user.favoriteBeers}}
        <a href="/beers/details/{{this._id}}">
            <div class="box-item">
                <div class="img-container">
                    <img src="{{this.image}}" alt="">
                </div>
                <div class="description">
                    <h2 class="title is-4">{{this.name}}</h2>
                    <h3 class="subtitle is-5">{{this.style}}</h3>
                    <p class="alc has-text-weight-semibold">ALC. {{this.alcohol}}% VOL</p>
                </div>
            </div>
        </a>
        {{/each}}

    </section>

    <section class="favoriteBeers">
        <h2 class="title is-4">Your favorite bars</h2>
        <hr>


        {{#each user.favoriteBars}}
        <a class="card-item bar-card" href="/bars/{{this._id}}">
            <div class="img-round">
                <img src="https://anamericaninrome.com/wp/wp-content/uploads/2017/06/caffe-corretto-3-1024x683.jpg"
                    alt="">
            </div>
            <div class="content">
                <p class="title is-4">{{this.name}}</p>
                <p class="subtitle">{{this.address}}</p>
            </div>
        </a>
        {{/each}}

    </section>

</body>

</html>